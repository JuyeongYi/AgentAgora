{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "받은 메시지는 작업 지시다 — 분석·확인 없이 실제로 수행(파일 편집·실행·검증)한 뒤 결과를 발신자에게 agora.dispatch로 회신하라. 처리할 작업이 없을 때만 agora.wait(timeout_ms=0)를 호출하라. 페르소나 규칙은 수신 명령에만 적용된다."
          }
        ]
      }
    ]
  }
}
